# Ripsynss

Documentation

```powershell
git clone <repo>
cd <repo>
```

```powershell
npm install / yarn install / pnpm install / bun install
npm run dev / yarn run dev / pnpm run dev / bun run dev
```

my recommendation is using "npm"

add your default meta tag for SEO support in src/components/SEO/SEOHeader.jsx
add more your specifik meta tag SEO support in components who call "SEOHeader.jsx"

# Use _ctrl_ + _alt_ + _F_ & search

```powershell
  <SEOHeader title="your_title" description="your_description" />
```

add more component from magicui & reactbits

constants folders for your information
public for your assets / image
