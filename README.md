# React + Vite app (Portfolio)

Documentation 

```powershell
git clone <repo>
cd <repo>
```

```powershell
npm install / yarn install / pnpm install
npm run dev
```
add your default meta tag for SEO support in src/components/SEO/SEOHeader.jsx
add more component from magicui & reactbits (already installed)
